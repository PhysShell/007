# Run 01 — the classifier's own summary, recorded verbatim

Reproduced exactly as returned, including its arithmetic. It is **not** the
source of any number in `results.md`: §3 makes the per-item records the data,
and this summary is recorded only because it was part of the output.

> Summary (primary cause only): 41 items, all classified — no compound, no
> unclear. r_broad = yes on 18 (002, 007, 010, 011, 014, 015, 016, 017, 019,
> 020, 021, 023, 025, 026, 027, 028, 031, 036, 037, 041 — 20, correcting the
> count); r_obs = yes on 5 (015, 028, 036, 037, 041), all a subset of the
> r_broad set.
>
> Two calls worth flagging: item_038 is deliberately no despite its
> identity-shaped fix — it is the exclusion's named case (an unresolved state
> for an ambiguous outcome, which the contract says is a different axis).
> item_031 is r_broad without r_obs: the receipt's dispatch/execution collapse
> is a grain identity defect, which the contract explicitly says need not be
> observation binding.

**Noted at recording time, before unblinding:** the summary states "yes on 18",
then lists twenty ids, then self-corrects to 20 in the same sentence. The
per-item records are authoritative and were counted mechanically.
