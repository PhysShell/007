# case-0002 R3.1 — source-complete authoritative replay (frozen)

> Corrects the R3 source-qualification defect. R3 (artifact `e7861f0c`, receipt
> `ec914b9a`) is preserved unchanged; see `r3-erratum.json`. Same code identity as
> R3 (adapter impl `e62ee735`, tree `aefc5a3e`), same command, unchanged v0 design.

## Why R3.1

The frozen R3 `report.json` recorded `coder-prefix: UNAVAILABLE` and derived only the
reviewer session — the durable CAS held the compressed coder gzip git blob but no
independently materialized object under the uncompressed extractor-input digest. So R3
proved untuned projection survival + determinism, but **not** a complete source-backed
compilation. R3.1 materialized the coder object and replayed.

## Phase 1 — coder object materialized

Decompressed the frozen `claude-session.prefix-r21.jsonl.gz` (git blob `6d8a09a7`,
21,239,298 gz bytes) → uncompressed **87,564,394 bytes**, sha256
`8392de803d600b19ac8273a43b2b766951d2eaf9a9197e1d8d43e97f541fca1c` (exact match) →
imported to CAS `0444`, round-trip verified, offsite snapshot `6717b270`
(`restic ls` confirmed). `manifest.yaml` NOT changed — it correctly names the uncompressed input.

## Phase 2 — fixture-demanded availability

| source | requested bytes | cas_resolvable | round_trip |
|---|---|---|---|
| reviewer-export | 5,963,644 | OK | true |
| coder-prefix | 87,564,394 | OK | true |
| plane-record | 94,084 | OK | true |

`negative_control: []` (empty by design). `source-selectors` requests both `reviewer` and `coder`.

## Two-run replay — DETERMINISTIC

Same command, output dir removed between runs. Complete **15-file** trees (now incl
`derived/coder.jsonl`) compared byte-for-byte: same file set, all bytes identical,
stdout & stderr identical. report.json = `7f72133abc88ff88…`.

## Multi-axis observation (recorded from the bytes)

```
generation:              PASS
source_compilation:      PASS            # reviewer + coder + plane-record all OK; both derived sessions
projection:              SURVIVED_UNTUNED   # 3/3 tasks valid, no missing kind/topic, no selector/schema change
task_dependence:         OBSERVED_UNDER_V0_TWO_TASK_LIMIT   # v0 pairwise resume-vs-audit; THIRD task excluded
evaluation:              UNAVAILABLE_UNDER_V0   # negative_control []; evaluation null
determinism:             DETERMINISTIC
overall_observation:     UNTUNED_PROJECTION_SURVIVED__SOURCE_COMPILATION_PASS__EVALUATION_UNAVAILABLE
deterministic_compilation_pipeline: PASS
development_result:      PARTIAL          # = no negative-control arm, NOT projection or compilation failure
```

Derived: 2 sessions (reviewer + coder), 2384 records, 5,339,109 bytes. Per-task: resume 6/valid,
audit 10/valid, impact 9/valid — all evaluation `null`.

## Static v0 limitations (recorded, not fixed)

first-two-summaries task-dependence; `budget-v0.yaml` unread; `required_relation_paths`
unevaluated; `forbidden_stale_as_current` unevaluated; no negative-control arm. **None fixed.**

The evidentiary base is now clean: both derived sessions present, `deterministic_compilation_pipeline: PASS`.
R4 evaluation-contract design can start on this base.
